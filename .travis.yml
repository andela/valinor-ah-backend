language: node_js
node_js:
  - "stable"
services:
  - postgresql 
cache:
  directories:
    - "node_modules"
before_script:
  - npm run db-migrate
after_script:
  - npm run db-undo-migrate
script:
  - npm run coveralls
